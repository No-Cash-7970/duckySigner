# Python Client for DApp Connect

## Installation

1. Install [uv package manager](https://docs.astral.sh/uv/) if it is not installed
2. Run `uv sync` to install packages

## Running example script

Run the interactive example script:

```sh
uv run main.py
```
